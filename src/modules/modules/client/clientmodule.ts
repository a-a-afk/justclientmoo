import { Category, Module } from "../../module";

export class ClientModule extends Module {
    constructor() {
        super("client", Category.CLIENT, "a module to edit settings in the client (tbd)")
    }
}